# Choferes - Sistema web de liquidación logística

Aplicación web full stack:

- `backend/`: API Express + PostgreSQL (`pg`) con autenticación JWT.
- `backend/migrations/001_init.sql`: esquema base (usuarios, choferes, viajes, liquidaciones).
- `frontend/`: Vite + React para operar el sistema.
